import { v as __toESM } from "../index.js";
import { c as AD, xt as P7 } from "./react-D5xHzNYo.js";
import "./i18next-CxGFEMD9.js";
import "./Trans-UjBkHktY.js";
import { b as useTranslation } from "./floating-ui.utils.dom-B62kDwTz.js";
import { t as tooltip_default } from "./tooltip-ZeeerRdq.js";
import "./app-icon-DPKtAo3e.js";
import "./useKeyPress-DtHtlpBY.js";
import { t as useStore } from "./store-BCKZaSV6.js";
import "./dist-mUzjAi-W.js";
import { i as useEventEmitterContextContext } from "./utils-pMdyvQan.js";
import "./use-breakpoints-Ij85IgkK.js";
import "./navigation-C_u5Brbk.js";
import { t as cn } from "./classnames-LHQnX9lX.js";
import "./divider-DZD56ykp.js";
import { ct as useNodes, h as uniqBy, jt as CUSTOM_NODE, mt as useStoreApi } from "./utils-DqpfZ3W-.js";
import "./declarations-DYMEe7hD.js";
import { Ft as NodeRunningStatus, Ot as BlockEnum, Vt as WorkflowRunningStatus } from "./config-Dopncj5R.js";
import { $n as useWorkflowStore, D as useSetWorkflowVarsWithValue, Kr as useInvalidAllLastRun, Qn as useStore$1, Ti as debounce, _ as useEdgesInteractionsWithoutSync, ar as setAutoFreeze, g as useNodesInteractionsWithoutSync, ir as produce, m as useWorkflowInteractions, nr as useHooksStore, u as useWorkflowRun } from "./hooks-Bj_4KA1b.js";
import { t as capitalize } from "./capitalize-DDeTGiHs.js";
import "./i18n-config-jsJ9YUFd.js";
import { T as toast, m as sseGet } from "./base-BuKAg6Le.js";
import "./src-BByrOQhU.js";
import "./markdown-utils-D9ZHqkyo.js";
import { c as getThreadMessages, d as chat_default, l as isValidGeneratedAnswer, n as getLastAnswer } from "./utils-Dep53bT6.js";
import { t as noop } from "./noop--k6oqy4d.js";
import "./app-context-BiSzFQEV.js";
import "./IconBase-CEF_UoQI.js";
import "./workflow-CULNEjcb.js";
import "./portal-to-follow-elem-DU-Awe3a.js";
import "./button-BqYXL-Bc.js";
import "./confirm-CKQP3hdx.js";
import { n as ActionButtonState, t as ActionButton } from "./action-button-BK9WUMzU.js";
import "./input-BOHL-qYi.js";
import "./switch-CFJdYxEh.js";
import "./select-C87OPWpf.js";
import "./loading-BryKikzY.js";
import "./dialog-DCCIZLOh.js";
import { t as Modal } from "./modal-9VFVByDR.js";
import "./dayjs.min-CNlMkMP6.js";
import "./utc-CCXXQ2Pp.js";
import { t as useTimestamp } from "./use-timestamp-DPYGivGq.js";
import "./toast-BY3OifkP.js";
import { u as TransferMethod } from "./app-DPjSIfgb.js";
import "./var-DPpHtpXT.js";
import "./credential-item-C22_bUSw.js";
import "./apps-b6zqLiv3.js";
import "./datasets-hfpkpjNW.js";
import "./use-base-DLpNl2rR.js";
import "./client-CLQTU2Oi.js";
import "./use-apps-t6Q_ZP4w.js";
import "./global-public-context-DpzzsplY.js";
import "./common-qRxT6xi0.js";
import "./i18n-lDm19WzR.js";
import "./share-Bnl8cbPg.js";
import "./useDebounceFn-Y4NS6c4S.js";
import "./checkbox-DTSgkYdc.js";
import { t as require_copy_to_clipboard } from "./copy-to-clipboard-CEvVSVWT.js";
import "./utils-J89sp7A8.js";
import "./images-D3Uv050C.js";
import "./common-fiYzNZ23.js";
import "./upgrade-btn-CpSPytX5.js";
import "./badge-DnA7gOK5.js";
import "./textarea-Ngtow1st.js";
import "./constants-P0J540jJ.js";
import "./modal-context-CphlqvPm.js";
import "./config-DUQ_rYOW.js";
import "./provider-context-CToT0pwd.js";
import "./link-1R-efKLW.js";
import "./use-refresh-plugin-list-C8aKRYG-.js";
import "./use-triggers-DguQxV9N.js";
import "./other-sSUV3aYW.js";
import "./dynamic-6-R69YAE.js";
import { m as submitHumanInputForm, n as fetchCurrentValueOfConversationVariable } from "./workflow-Dbk_had1.js";
import "./status-CPKUa4Vg.js";
import "./fast-deep-equal-T8EktbR5.js";
import "./format-BCVnrx09.js";
import "./general-C8w9hIeL.js";
import "./prompt-editor-PJuCbabP.js";
import "./pipeline-EQygYMXw.js";
import "./hooks-BTelimHS.js";
import "./use-common-BJ1aZaXJ.js";
import "./popover-CTEbJchz.js";
import "./model-selector-ahzffqOG.js";
import "./dist-CGvyUAMR.js";
import "./use-theme-3YxPQV4B.js";
import "./model-name-DBTGJc7a.js";
import "./skeleton-Brd40zkS.js";
import "./alert-dialog-_9biHzKe.js";
import "./alertsAndFeedback-BpcdEYpr.js";
import "./amplitude-BnPDazhN.js";
import "./esm-CfW7DN4f.js";
import "./radio-4K5OYxdz.js";
import "./modal-C9_LwvXw.js";
import "./use-get-icon-D8ihnTYG.js";
import { S as stopChatMessageResponding, _ as fetchSuggestedQuestions, t as code_editor_default } from "./code-editor-BPvpmrXg.js";
import { c as getProcessedFilesFromResponse, s as getProcessedFiles } from "./utils-Dh7W533Q.js";
import { t as CodeLanguage } from "./types-C18Ag7fa.js";
import "./tag-input-9kTFEfAj.js";
import "./dist-DV9-MooO2.js";
import "./image-preview-D0J1Go0B.js";
import { I as Icon$1, L as Icon } from "./dropdown-menu-DdKnIBZG.js";
import { t as Icon$2 } from "./arrows-fbeBWnky.js";
import "./var-reference-picker-PlAsXjUc.js";
import { s as Icon$3 } from "./others-CYlDNVYO.js";
import "./Warning-BiM5Lre1.js";
import { o as useResizePanel, t as useMount } from "./useMount-BZpS-GRB.js";
import "./check-input-forms-hooks-D5-pytt0.js";
import "./relativeTime-BAkB0A3u.js";
import "./file-uploader-DuOqY7O5.js";
import { n as processOpeningStatement, t as getProcessedInputs } from "./utils-x6-EORb9.js";
import { t as form_item_default } from "./form-item-VN4UVnIP.js";
import { n as useFeatures } from "./audio.player.manager-BbxYn4nN.js";
import "./files-Do6W6gRz.js";
import "./context-X_lHIALZ.js";
import "./log-C5ooGRGD.js";
import "./loading-anim-Ck_Modzp.js";
import { i as Icon$4 } from "./usage-CsZ9mdCY.js";
import "./grid-mask-BR99s5KY.js";
import "./progress-bar-B_Dpla84.js";
import "./usage-info-T9FNDAad.js";
import "./annotation-D2xI61pO.js";
import "./features-D2eB-UM8.js";
import { t as ChatVarType } from "./type-DLUzUsVc.js";
import * as React$1 from "react";
import { memo, useCallback, useEffect, useImperativeHandle, useMemo, useRef, useState } from "react";
import { Fragment as Fragment$1, jsx, jsxs } from "react/jsx-runtime";
//#region app/components/workflow/panel/debug-and-preview/conversation-variable-modal.tsx
var import_copy_to_clipboard = /* @__PURE__ */ __toESM(require_copy_to_clipboard(), 1);
var ConversationVariableModal = ({ conversationID, onHide }) => {
	const { t } = useTranslation();
	const { formatTime } = useTimestamp();
	const varList = useStore$1((s) => s.conversationVariables);
	const appID = useStore$1((s) => s.appId);
	const [currentVar, setCurrentVar] = React$1.useState(varList[0]);
	const [latestValueMap, setLatestValueMap] = React$1.useState({});
	const [latestValueTimestampMap, setLatestValueTimestampMap] = React$1.useState({});
	const getChatVarLatestValues = useCallback(async () => {
		if (conversationID && varList.length > 0) {
			const res = await fetchCurrentValueOfConversationVariable({
				url: `/apps/${appID}/conversation-variables`,
				params: { conversation_id: conversationID }
			});
			if (res.data.length > 0) {
				setLatestValueMap(res.data.reduce((acc, cur) => {
					acc[cur.id] = cur.value;
					return acc;
				}, {}));
				setLatestValueTimestampMap(res.data.reduce((acc, cur) => {
					acc[cur.id] = cur.updated_at;
					return acc;
				}, {}));
			}
		}
	}, [
		appID,
		conversationID,
		varList.length
	]);
	const [isCopied, setIsCopied] = React$1.useState(false);
	const handleCopy = useCallback(() => {
		(0, import_copy_to_clipboard.default)(currentVar.value);
		setIsCopied(true);
		setTimeout(() => {
			setIsCopied(false);
		}, 2e3);
	}, [currentVar.value]);
	useMount(() => {
		getChatVarLatestValues();
	});
	return /* @__PURE__ */ jsxs(Modal, {
		isShow: true,
		onClose: noop,
		className: cn("h-[640px] w-[920px] max-w-[920px] p-0"),
		children: [/* @__PURE__ */ jsx("div", {
			className: "absolute right-4 top-4 cursor-pointer p-2",
			onClick: onHide,
			children: /* @__PURE__ */ jsx(P7, { className: "h-4 w-4 text-text-tertiary" })
		}), /* @__PURE__ */ jsxs("div", {
			className: "flex h-full w-full",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "flex h-full w-[224px] shrink-0 flex-col border-r border-divider-burn bg-background-sidenav-bg",
				children: [/* @__PURE__ */ jsx("div", {
					className: "system-xl-semibold shrink-0 pb-3 pl-5 pr-4 pt-5 text-text-primary",
					children: t("chatVariable.panelTitle", { ns: "workflow" })
				}), /* @__PURE__ */ jsx("div", {
					className: "grow overflow-y-auto px-3 py-2",
					children: varList.map((chatVar) => /* @__PURE__ */ jsxs("div", {
						className: cn("radius-md group mb-0.5 flex cursor-pointer items-center p-2 hover:bg-state-base-hover", currentVar.id === chatVar.id && "bg-state-base-hover"),
						onClick: () => setCurrentVar(chatVar),
						children: [/* @__PURE__ */ jsx(Icon$3, { className: cn("mr-1 h-4 w-4 shrink-0 text-text-tertiary group-hover:text-util-colors-teal-teal-700", currentVar.id === chatVar.id && "text-util-colors-teal-teal-700") }), /* @__PURE__ */ jsx("div", {
							title: chatVar.name,
							className: cn("system-sm-medium truncate text-text-tertiary group-hover:text-util-colors-teal-teal-700", currentVar.id === chatVar.id && "text-util-colors-teal-teal-700"),
							children: chatVar.name
						})]
					}, chatVar.id))
				})]
			}), /* @__PURE__ */ jsxs("div", {
				className: "flex h-full w-0 grow flex-col bg-components-panel-bg",
				children: [/* @__PURE__ */ jsx("div", {
					className: "shrink-0 p-4 pb-2",
					children: /* @__PURE__ */ jsxs("div", {
						className: "flex items-center gap-1 py-1",
						children: [/* @__PURE__ */ jsx("div", {
							className: "system-xl-semibold text-text-primary",
							children: currentVar.name
						}), /* @__PURE__ */ jsx("div", {
							className: "system-xs-medium text-text-tertiary",
							children: capitalize(currentVar.value_type)
						})]
					})
				}), /* @__PURE__ */ jsxs("div", {
					className: "flex h-0 grow flex-col p-4 pt-2",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "mb-2 flex shrink-0 items-center gap-2",
						children: [
							/* @__PURE__ */ jsx("div", {
								className: "system-xs-medium-uppercase shrink-0 text-text-tertiary",
								children: t("chatVariable.storedContent", { ns: "workflow" }).toLocaleUpperCase()
							}),
							/* @__PURE__ */ jsx("div", {
								className: "h-px grow",
								style: { background: "linear-gradient(to right, rgba(16, 24, 40, 0.08) 0%, rgba(255, 255, 255) 100%)" }
							}),
							!!latestValueTimestampMap[currentVar.id] && /* @__PURE__ */ jsxs("div", {
								className: "system-xs-regular shrink-0 text-text-tertiary",
								children: [t("chatVariable.updatedAt", { ns: "workflow" }), formatTime(latestValueTimestampMap[currentVar.id], t("dateTimeFormat", { ns: "appLog" }))]
							})
						]
					}), /* @__PURE__ */ jsxs("div", {
						className: "grow overflow-y-auto",
						children: [currentVar.value_type !== ChatVarType.Number && currentVar.value_type !== ChatVarType.String && /* @__PURE__ */ jsxs("div", {
							className: "flex h-full flex-col rounded-lg bg-components-input-bg-normal px-2 pb-2",
							children: [/* @__PURE__ */ jsxs("div", {
								className: "flex h-7 shrink-0 items-center justify-between pl-3 pr-2 pt-1",
								children: [/* @__PURE__ */ jsx("div", {
									className: "system-xs-semibold text-text-secondary",
									children: "JSON"
								}), /* @__PURE__ */ jsx("div", {
									className: "flex items-center p-1",
									children: !isCopied ? /* @__PURE__ */ jsx(Icon, {
										className: "h-4 w-4 cursor-pointer text-text-tertiary",
										onClick: handleCopy
									}) : /* @__PURE__ */ jsx(Icon$1, { className: "h-4 w-4 text-text-tertiary" })
								})]
							}), /* @__PURE__ */ jsx("div", {
								className: "grow pl-4",
								children: /* @__PURE__ */ jsx(code_editor_default, {
									readOnly: true,
									noWrapper: true,
									isExpand: true,
									language: CodeLanguage.json,
									value: latestValueMap[currentVar.id] || "",
									isJSONStringifyBeauty: true
								})
							})]
						}), (currentVar.value_type === ChatVarType.Number || currentVar.value_type === ChatVarType.String) && /* @__PURE__ */ jsx("div", {
							className: "system-md-regular h-full overflow-y-auto overflow-x-hidden rounded-lg bg-components-input-bg-normal px-4 py-3 text-components-input-text-filled",
							children: latestValueMap[currentVar.id] || ""
						})]
					})]
				})]
			})]
		})]
	});
};
//#endregion
//#region app/components/workflow/panel/debug-and-preview/empty.tsx
var Empty = () => {
	const { t } = useTranslation();
	return /* @__PURE__ */ jsxs("div", {
		className: "absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2",
		children: [/* @__PURE__ */ jsx("div", {
			className: "mb-2 flex justify-center",
			children: /* @__PURE__ */ jsx(Icon$4, { className: "h-12 w-12 text-gray-300" })
		}), /* @__PURE__ */ jsx("div", {
			className: "w-[256px] text-center text-[13px] text-gray-400",
			children: t("common.previewPlaceholder", { ns: "workflow" })
		})]
	});
};
//#endregion
//#region app/components/workflow/panel/debug-and-preview/hooks.ts
var useChat = (config, formSettings, prevChatTree, stopChat) => {
	const { t } = useTranslation();
	const { handleRun } = useWorkflowRun();
	const hasStopResponded = useRef(false);
	const workflowStore = useWorkflowStore();
	const conversationId = useRef("");
	const taskIdRef = useRef("");
	const [isResponding, setIsResponding] = useState(false);
	const isRespondingRef = useRef(false);
	const workflowEventsAbortControllerRef = useRef(null);
	const configsMap = useHooksStore((s) => s.configsMap);
	const invalidAllLastRun = useInvalidAllLastRun(configsMap?.flowType, configsMap?.flowId);
	const { fetchInspectVars } = useSetWorkflowVarsWithValue();
	const [suggestedQuestions, setSuggestQuestions] = useState([]);
	const suggestedQuestionsAbortControllerRef = useRef(null);
	const { setIterTimes, setLoopTimes } = workflowStore.getState();
	const store = useStoreApi();
	const handleResponding = useCallback((isResponding) => {
		setIsResponding(isResponding);
		isRespondingRef.current = isResponding;
	}, []);
	const [chatTree, setChatTree] = useState(prevChatTree || []);
	const chatTreeRef = useRef(chatTree);
	const [targetMessageId, setTargetMessageId] = useState();
	const threadMessages = useMemo(() => getThreadMessages(chatTree, targetMessageId), [chatTree, targetMessageId]);
	const getIntroduction = useCallback((str) => {
		return processOpeningStatement(str, formSettings?.inputs || {}, formSettings?.inputsForm || []);
	}, [formSettings?.inputs, formSettings?.inputsForm]);
	const processedOpeningContent = config?.opening_statement ? getIntroduction(config.opening_statement) : void 0;
	const processedSuggestionsKey = config?.suggested_questions ? JSON.stringify(config.suggested_questions.map((q) => getIntroduction(q))) : void 0;
	const openingStatementItem = useMemo(() => {
		if (!processedOpeningContent) return null;
		return {
			id: "opening-statement",
			content: processedOpeningContent,
			isAnswer: true,
			isOpeningStatement: true,
			suggestedQuestions: processedSuggestionsKey ? JSON.parse(processedSuggestionsKey) : void 0
		};
	}, [processedOpeningContent, processedSuggestionsKey]);
	const threadOpener = useMemo(() => threadMessages.find((item) => item.isOpeningStatement) ?? null, [threadMessages]);
	const mergedOpeningItem = useMemo(() => {
		if (!threadOpener || !openingStatementItem) return null;
		return {
			...threadOpener,
			content: openingStatementItem.content,
			suggestedQuestions: openingStatementItem.suggestedQuestions
		};
	}, [threadOpener, openingStatementItem]);
	/** Final chat list that will be rendered */
	const chatList = useMemo(() => {
		const ret = [...threadMessages];
		if (openingStatementItem) {
			const index = threadMessages.findIndex((item) => item.isOpeningStatement);
			if (index > -1 && mergedOpeningItem) ret[index] = mergedOpeningItem;
			else if (index === -1) ret.unshift(openingStatementItem);
		}
		return ret;
	}, [
		threadMessages,
		openingStatementItem,
		mergedOpeningItem
	]);
	useEffect(() => {
		setAutoFreeze(false);
		return () => {
			setAutoFreeze(true);
		};
	}, []);
	/** Find the target node by bfs and then operate on it */
	const produceChatTreeNode = useCallback((targetId, operation) => {
		return produce(chatTreeRef.current, (draft) => {
			const queue = [...draft];
			while (queue.length > 0) {
				const current = queue.shift();
				if (current.id === targetId) {
					operation(current);
					break;
				}
				if (current.children) queue.push(...current.children);
			}
		});
	}, []);
	const updateChatTreeNode = useCallback((id, fieldsOrUpdate) => {
		const nextState = produceChatTreeNode(id, (node) => {
			if (typeof fieldsOrUpdate === "function") fieldsOrUpdate(node);
			else Object.keys(fieldsOrUpdate).forEach((key) => {
				node[key] = fieldsOrUpdate[key];
			});
		});
		setChatTree(nextState);
		chatTreeRef.current = nextState;
	}, [produceChatTreeNode]);
	const handleStop = useCallback(() => {
		hasStopResponded.current = true;
		handleResponding(false);
		if (stopChat && taskIdRef.current) stopChat(taskIdRef.current);
		setIterTimes(1);
		setLoopTimes(1);
		if (suggestedQuestionsAbortControllerRef.current) suggestedQuestionsAbortControllerRef.current.abort();
		if (workflowEventsAbortControllerRef.current) workflowEventsAbortControllerRef.current.abort();
	}, [
		handleResponding,
		setIterTimes,
		setLoopTimes,
		stopChat
	]);
	const handleRestart = useCallback(() => {
		conversationId.current = "";
		taskIdRef.current = "";
		handleStop();
		setIterTimes(1);
		setLoopTimes(1);
		setChatTree([]);
		setSuggestQuestions([]);
	}, [
		handleStop,
		setIterTimes,
		setLoopTimes
	]);
	const updateCurrentQAOnTree = useCallback(({ parentId, responseItem, placeholderQuestionId, questionItem }) => {
		let nextState;
		const currentQA = {
			...questionItem,
			children: [{
				...responseItem,
				children: []
			}]
		};
		if (!parentId && !chatTree.some((item) => [placeholderQuestionId, questionItem.id].includes(item.id))) nextState = produce(chatTree, (draft) => {
			draft.push(currentQA);
		});
		else nextState = produceChatTreeNode(parentId, (parentNode) => {
			const questionNodeIndex = parentNode.children.findIndex((item) => [placeholderQuestionId, questionItem.id].includes(item.id));
			if (questionNodeIndex === -1) parentNode.children.push(currentQA);
			else parentNode.children[questionNodeIndex] = currentQA;
		});
		setChatTree(nextState);
		chatTreeRef.current = nextState;
	}, [chatTree, produceChatTreeNode]);
	const handleSend = useCallback((params, { onGetSuggestedQuestions }) => {
		if (isRespondingRef.current) {
			toast.info(t("errorMessage.waitForResponse", { ns: "appDebug" }));
			return false;
		}
		if (workflowEventsAbortControllerRef.current) workflowEventsAbortControllerRef.current.abort();
		const parentMessage = threadMessages.find((item) => item.id === params.parent_message_id);
		const placeholderQuestionId = `question-${Date.now()}`;
		const questionItem = {
			id: placeholderQuestionId,
			content: params.query,
			isAnswer: false,
			message_files: params.files,
			parentMessageId: params.parent_message_id
		};
		const placeholderAnswerId = `answer-placeholder-${Date.now()}`;
		const placeholderAnswerItem = {
			id: placeholderAnswerId,
			content: "",
			isAnswer: true,
			parentMessageId: questionItem.id,
			siblingIndex: parentMessage?.children?.length ?? chatTree.length
		};
		setTargetMessageId(parentMessage?.id);
		updateCurrentQAOnTree({
			parentId: params.parent_message_id,
			responseItem: placeholderAnswerItem,
			placeholderQuestionId,
			questionItem
		});
		const responseItem = {
			id: placeholderAnswerId,
			content: "",
			agent_thoughts: [],
			message_files: [],
			isAnswer: true,
			parentMessageId: questionItem.id,
			siblingIndex: parentMessage?.children?.length ?? chatTree.length,
			humanInputFormDataList: [],
			humanInputFilledFormDataList: []
		};
		handleResponding(true);
		const { files, inputs, ...restParams } = params;
		const bodyParams = {
			files: getProcessedFiles(files || []),
			inputs: getProcessedInputs(inputs || {}, formSettings?.inputsForm || []),
			...restParams
		};
		if (bodyParams?.files?.length) bodyParams.files = bodyParams.files.map((item) => {
			if (item.transfer_method === TransferMethod.local_file) return {
				...item,
				url: ""
			};
			return item;
		});
		let hasSetResponseId = false;
		handleRun(bodyParams, {
			getAbortController: (abortController) => {
				workflowEventsAbortControllerRef.current = abortController;
			},
			onData: (message, isFirstMessage, { conversationId: newConversationId, messageId, taskId }) => {
				responseItem.content = responseItem.content + message;
				if (messageId && !hasSetResponseId) {
					questionItem.id = `question-${messageId}`;
					responseItem.id = messageId;
					responseItem.parentMessageId = questionItem.id;
					hasSetResponseId = true;
				}
				if (isFirstMessage && newConversationId) conversationId.current = newConversationId;
				taskIdRef.current = taskId;
				if (messageId) responseItem.id = messageId;
				updateCurrentQAOnTree({
					placeholderQuestionId,
					questionItem,
					responseItem,
					parentId: params.parent_message_id
				});
			},
			async onCompleted(hasError, errorMessage) {
				const { workflowRunningData } = workflowStore.getState();
				handleResponding(false);
				if (workflowRunningData?.result.status !== WorkflowRunningStatus.Paused) {
					fetchInspectVars({});
					invalidAllLastRun();
					if (hasError) {
						if (errorMessage) {
							responseItem.content = errorMessage;
							responseItem.isError = true;
							updateCurrentQAOnTree({
								placeholderQuestionId,
								questionItem,
								responseItem,
								parentId: params.parent_message_id
							});
						}
						return;
					}
					if (config?.suggested_questions_after_answer?.enabled && !hasStopResponded.current && onGetSuggestedQuestions) try {
						const { data } = await onGetSuggestedQuestions(responseItem.id, (newAbortController) => suggestedQuestionsAbortControllerRef.current = newAbortController);
						setSuggestQuestions(data);
					} catch (error) {
						setSuggestQuestions([]);
					}
				}
			},
			onMessageEnd: (messageEnd) => {
				responseItem.citation = messageEnd.metadata?.retriever_resources || [];
				const processedFilesFromResponse = getProcessedFilesFromResponse(messageEnd.files || []);
				responseItem.allFiles = uniqBy([...responseItem.allFiles || [], ...processedFilesFromResponse || []], "id");
				updateCurrentQAOnTree({
					placeholderQuestionId,
					questionItem,
					responseItem,
					parentId: params.parent_message_id
				});
			},
			onMessageReplace: (messageReplace) => {
				responseItem.content = messageReplace.answer;
			},
			onError() {
				handleResponding(false);
			},
			onWorkflowStarted: ({ workflow_run_id, task_id, conversation_id, message_id }) => {
				if (conversation_id) conversationId.current = conversation_id;
				if (message_id && !hasSetResponseId) {
					questionItem.id = `question-${message_id}`;
					responseItem.id = message_id;
					responseItem.parentMessageId = questionItem.id;
					hasSetResponseId = true;
				}
				if (responseItem.workflowProcess && responseItem.workflowProcess.tracing.length > 0) {
					handleResponding(true);
					responseItem.workflowProcess.status = WorkflowRunningStatus.Running;
				} else {
					taskIdRef.current = task_id;
					responseItem.workflow_run_id = workflow_run_id;
					responseItem.workflowProcess = {
						status: WorkflowRunningStatus.Running,
						tracing: []
					};
				}
				updateCurrentQAOnTree({
					placeholderQuestionId,
					questionItem,
					responseItem,
					parentId: params.parent_message_id
				});
			},
			onWorkflowFinished: ({ data }) => {
				responseItem.workflowProcess.status = data.status;
				updateCurrentQAOnTree({
					placeholderQuestionId,
					questionItem,
					responseItem,
					parentId: params.parent_message_id
				});
			},
			onIterationStart: ({ data }) => {
				responseItem.workflowProcess.tracing.push({
					...data,
					status: NodeRunningStatus.Running
				});
				updateCurrentQAOnTree({
					placeholderQuestionId,
					questionItem,
					responseItem,
					parentId: params.parent_message_id
				});
			},
			onIterationFinish: ({ data }) => {
				const currentTracingIndex = responseItem.workflowProcess.tracing.findIndex((item) => item.id === data.id);
				if (currentTracingIndex > -1) {
					responseItem.workflowProcess.tracing[currentTracingIndex] = {
						...responseItem.workflowProcess.tracing[currentTracingIndex],
						...data
					};
					updateCurrentQAOnTree({
						placeholderQuestionId,
						questionItem,
						responseItem,
						parentId: params.parent_message_id
					});
				}
			},
			onLoopStart: ({ data }) => {
				responseItem.workflowProcess.tracing.push({
					...data,
					status: NodeRunningStatus.Running
				});
				updateCurrentQAOnTree({
					placeholderQuestionId,
					questionItem,
					responseItem,
					parentId: params.parent_message_id
				});
			},
			onLoopFinish: ({ data }) => {
				const currentTracingIndex = responseItem.workflowProcess.tracing.findIndex((item) => item.id === data.id);
				if (currentTracingIndex > -1) {
					responseItem.workflowProcess.tracing[currentTracingIndex] = {
						...responseItem.workflowProcess.tracing[currentTracingIndex],
						...data
					};
					updateCurrentQAOnTree({
						placeholderQuestionId,
						questionItem,
						responseItem,
						parentId: params.parent_message_id
					});
				}
			},
			onNodeStarted: ({ data }) => {
				const currentIndex = responseItem.workflowProcess.tracing.findIndex((item) => item.node_id === data.node_id);
				if (currentIndex > -1) responseItem.workflowProcess.tracing[currentIndex] = {
					...data,
					status: NodeRunningStatus.Running
				};
				else responseItem.workflowProcess.tracing.push({
					...data,
					status: NodeRunningStatus.Running
				});
				updateCurrentQAOnTree({
					placeholderQuestionId,
					questionItem,
					responseItem,
					parentId: params.parent_message_id
				});
			},
			onNodeRetry: ({ data }) => {
				responseItem.workflowProcess.tracing.push(data);
				updateCurrentQAOnTree({
					placeholderQuestionId,
					questionItem,
					responseItem,
					parentId: params.parent_message_id
				});
			},
			onNodeFinished: ({ data }) => {
				const currentTracingIndex = responseItem.workflowProcess.tracing.findIndex((item) => item.id === data.id);
				if (currentTracingIndex > -1) {
					responseItem.workflowProcess.tracing[currentTracingIndex] = {
						...responseItem.workflowProcess.tracing[currentTracingIndex],
						...data
					};
					updateCurrentQAOnTree({
						placeholderQuestionId,
						questionItem,
						responseItem,
						parentId: params.parent_message_id
					});
				}
			},
			onAgentLog: ({ data }) => {
				const currentNodeIndex = responseItem.workflowProcess.tracing.findIndex((item) => item.node_id === data.node_id);
				if (currentNodeIndex > -1) {
					const current = responseItem.workflowProcess.tracing[currentNodeIndex];
					if (current.execution_metadata) if (current.execution_metadata.agent_log) {
						const currentLogIndex = current.execution_metadata.agent_log.findIndex((log) => log.message_id === data.message_id);
						if (currentLogIndex > -1) current.execution_metadata.agent_log[currentLogIndex] = {
							...current.execution_metadata.agent_log[currentLogIndex],
							...data
						};
						else current.execution_metadata.agent_log.push(data);
					} else current.execution_metadata.agent_log = [data];
					else current.execution_metadata = { agent_log: [data] };
					responseItem.workflowProcess.tracing[currentNodeIndex] = { ...current };
					updateCurrentQAOnTree({
						placeholderQuestionId,
						questionItem,
						responseItem,
						parentId: params.parent_message_id
					});
				}
			},
			onHumanInputRequired: ({ data }) => {
				if (!responseItem.humanInputFormDataList) responseItem.humanInputFormDataList = [data];
				else {
					const currentFormIndex = responseItem.humanInputFormDataList.findIndex((item) => item.node_id === data.node_id);
					if (currentFormIndex > -1) responseItem.humanInputFormDataList[currentFormIndex] = data;
					else responseItem.humanInputFormDataList.push(data);
				}
				const currentTracingIndex = responseItem.workflowProcess.tracing.findIndex((item) => item.node_id === data.node_id);
				if (currentTracingIndex > -1) {
					responseItem.workflowProcess.tracing[currentTracingIndex].status = NodeRunningStatus.Paused;
					updateCurrentQAOnTree({
						placeholderQuestionId,
						questionItem,
						responseItem,
						parentId: params.parent_message_id
					});
				}
			},
			onHumanInputFormFilled: ({ data }) => {
				if (responseItem.humanInputFormDataList?.length) {
					const currentFormIndex = responseItem.humanInputFormDataList.findIndex((item) => item.node_id === data.node_id);
					responseItem.humanInputFormDataList.splice(currentFormIndex, 1);
				}
				if (!responseItem.humanInputFilledFormDataList) responseItem.humanInputFilledFormDataList = [data];
				else responseItem.humanInputFilledFormDataList.push(data);
				updateCurrentQAOnTree({
					placeholderQuestionId,
					questionItem,
					responseItem,
					parentId: params.parent_message_id
				});
			},
			onHumanInputFormTimeout: ({ data }) => {
				if (responseItem.humanInputFormDataList?.length) {
					const currentFormIndex = responseItem.humanInputFormDataList.findIndex((item) => item.node_id === data.node_id);
					responseItem.humanInputFormDataList[currentFormIndex].expiration_time = data.expiration_time;
				}
				updateCurrentQAOnTree({
					placeholderQuestionId,
					questionItem,
					responseItem,
					parentId: params.parent_message_id
				});
			},
			onWorkflowPaused: ({ data: _data }) => {
				responseItem.workflowProcess.status = WorkflowRunningStatus.Paused;
				updateCurrentQAOnTree({
					placeholderQuestionId,
					questionItem,
					responseItem,
					parentId: params.parent_message_id
				});
			}
		});
	}, [
		threadMessages,
		chatTree.length,
		updateCurrentQAOnTree,
		handleResponding,
		formSettings?.inputsForm,
		handleRun,
		t,
		workflowStore,
		fetchInspectVars,
		invalidAllLastRun,
		config?.suggested_questions_after_answer?.enabled
	]);
	const handleSubmitHumanInputForm = async (formToken, formData) => {
		await submitHumanInputForm(formToken, formData);
	};
	const getHumanInputNodeData = (nodeID) => {
		const { getNodes } = store.getState();
		return getNodes().filter((node) => node.type === CUSTOM_NODE).find((n) => n.id === nodeID);
	};
	const handleResume = useCallback((messageId, workflowRunId, { onGetSuggestedQuestions }) => {
		const url = `/workflow/${workflowRunId}/events?include_state_snapshot=true`;
		const otherOptions = {
			getAbortController: (abortController) => {
				workflowEventsAbortControllerRef.current = abortController;
			},
			onData: (message, _isFirstMessage, { conversationId: newConversationId, messageId: msgId, taskId }) => {
				updateChatTreeNode(messageId, (responseItem) => {
					responseItem.content = responseItem.content + message;
					if (msgId) responseItem.id = msgId;
				});
				if (newConversationId) conversationId.current = newConversationId;
				if (taskId) taskIdRef.current = taskId;
			},
			async onCompleted(hasError) {
				const { workflowRunningData } = workflowStore.getState();
				handleResponding(false);
				if (workflowRunningData?.result.status !== WorkflowRunningStatus.Paused) {
					fetchInspectVars({});
					invalidAllLastRun();
					if (hasError) return;
					if (config?.suggested_questions_after_answer?.enabled && !hasStopResponded.current && onGetSuggestedQuestions) try {
						const { data } = await onGetSuggestedQuestions(messageId, (newAbortController) => suggestedQuestionsAbortControllerRef.current = newAbortController);
						setSuggestQuestions(data);
					} catch {
						setSuggestQuestions([]);
					}
				}
			},
			onMessageEnd: (messageEnd) => {
				updateChatTreeNode(messageId, (responseItem) => {
					responseItem.citation = messageEnd.metadata?.retriever_resources || [];
					const processedFilesFromResponse = getProcessedFilesFromResponse(messageEnd.files || []);
					responseItem.allFiles = uniqBy([...responseItem.allFiles || [], ...processedFilesFromResponse || []], "id");
				});
			},
			onMessageReplace: (messageReplace) => {
				updateChatTreeNode(messageId, (responseItem) => {
					responseItem.content = messageReplace.answer;
				});
			},
			onError() {
				handleResponding(false);
			},
			onWorkflowStarted: ({ workflow_run_id, task_id }) => {
				handleResponding(true);
				hasStopResponded.current = false;
				updateChatTreeNode(messageId, (responseItem) => {
					if (responseItem.workflowProcess && responseItem.workflowProcess.tracing.length > 0) responseItem.workflowProcess.status = WorkflowRunningStatus.Running;
					else {
						taskIdRef.current = task_id;
						responseItem.workflow_run_id = workflow_run_id;
						responseItem.workflowProcess = {
							status: WorkflowRunningStatus.Running,
							tracing: []
						};
					}
				});
			},
			onWorkflowFinished: ({ data: workflowFinishedData }) => {
				updateChatTreeNode(messageId, (responseItem) => {
					if (responseItem.workflowProcess) responseItem.workflowProcess.status = workflowFinishedData.status;
				});
			},
			onIterationStart: ({ data: iterationStartedData }) => {
				updateChatTreeNode(messageId, (responseItem) => {
					if (!responseItem.workflowProcess) return;
					if (!responseItem.workflowProcess.tracing) responseItem.workflowProcess.tracing = [];
					responseItem.workflowProcess.tracing.push({
						...iterationStartedData,
						status: WorkflowRunningStatus.Running
					});
				});
			},
			onIterationFinish: ({ data: iterationFinishedData }) => {
				updateChatTreeNode(messageId, (responseItem) => {
					if (!responseItem.workflowProcess?.tracing) return;
					const tracing = responseItem.workflowProcess.tracing;
					const iterationIndex = tracing.findIndex((item) => item.node_id === iterationFinishedData.node_id && (item.execution_metadata?.parallel_id === iterationFinishedData.execution_metadata?.parallel_id || item.parallel_id === iterationFinishedData.execution_metadata?.parallel_id));
					if (iterationIndex > -1) tracing[iterationIndex] = {
						...tracing[iterationIndex],
						...iterationFinishedData,
						status: WorkflowRunningStatus.Succeeded
					};
				});
			},
			onNodeStarted: ({ data: nodeStartedData }) => {
				updateChatTreeNode(messageId, (responseItem) => {
					if (!responseItem.workflowProcess) return;
					if (!responseItem.workflowProcess.tracing) responseItem.workflowProcess.tracing = [];
					const currentIndex = responseItem.workflowProcess.tracing.findIndex((item) => item.node_id === nodeStartedData.node_id);
					if (currentIndex > -1) responseItem.workflowProcess.tracing[currentIndex] = {
						...nodeStartedData,
						status: NodeRunningStatus.Running
					};
					else {
						if (nodeStartedData.iteration_id) return;
						responseItem.workflowProcess.tracing.push({
							...nodeStartedData,
							status: WorkflowRunningStatus.Running
						});
					}
				});
			},
			onNodeFinished: ({ data: nodeFinishedData }) => {
				updateChatTreeNode(messageId, (responseItem) => {
					if (!responseItem.workflowProcess?.tracing) return;
					if (nodeFinishedData.iteration_id) return;
					const currentIndex = responseItem.workflowProcess.tracing.findIndex((item) => {
						if (!item.execution_metadata?.parallel_id) return item.id === nodeFinishedData.id;
						return item.id === nodeFinishedData.id && item.execution_metadata?.parallel_id === nodeFinishedData.execution_metadata?.parallel_id;
					});
					if (currentIndex > -1) responseItem.workflowProcess.tracing[currentIndex] = nodeFinishedData;
				});
			},
			onLoopStart: ({ data: loopStartedData }) => {
				updateChatTreeNode(messageId, (responseItem) => {
					if (!responseItem.workflowProcess) return;
					if (!responseItem.workflowProcess.tracing) responseItem.workflowProcess.tracing = [];
					responseItem.workflowProcess.tracing.push({
						...loopStartedData,
						status: WorkflowRunningStatus.Running
					});
				});
			},
			onLoopFinish: ({ data: loopFinishedData }) => {
				updateChatTreeNode(messageId, (responseItem) => {
					if (!responseItem.workflowProcess?.tracing) return;
					const tracing = responseItem.workflowProcess.tracing;
					const loopIndex = tracing.findIndex((item) => item.node_id === loopFinishedData.node_id && (item.execution_metadata?.parallel_id === loopFinishedData.execution_metadata?.parallel_id || item.parallel_id === loopFinishedData.execution_metadata?.parallel_id));
					if (loopIndex > -1) tracing[loopIndex] = {
						...tracing[loopIndex],
						...loopFinishedData,
						status: WorkflowRunningStatus.Succeeded
					};
				});
			},
			onHumanInputRequired: ({ data: humanInputRequiredData }) => {
				updateChatTreeNode(messageId, (responseItem) => {
					if (!responseItem.humanInputFormDataList) responseItem.humanInputFormDataList = [humanInputRequiredData];
					else {
						const currentFormIndex = responseItem.humanInputFormDataList.findIndex((item) => item.node_id === humanInputRequiredData.node_id);
						if (currentFormIndex > -1) responseItem.humanInputFormDataList[currentFormIndex] = humanInputRequiredData;
						else responseItem.humanInputFormDataList.push(humanInputRequiredData);
					}
					if (responseItem.workflowProcess?.tracing) {
						const currentTracingIndex = responseItem.workflowProcess.tracing.findIndex((item) => item.node_id === humanInputRequiredData.node_id);
						if (currentTracingIndex > -1) responseItem.workflowProcess.tracing[currentTracingIndex].status = NodeRunningStatus.Paused;
					}
				});
			},
			onHumanInputFormFilled: ({ data: humanInputFilledFormData }) => {
				updateChatTreeNode(messageId, (responseItem) => {
					if (responseItem.humanInputFormDataList?.length) {
						const currentFormIndex = responseItem.humanInputFormDataList.findIndex((item) => item.node_id === humanInputFilledFormData.node_id);
						if (currentFormIndex > -1) responseItem.humanInputFormDataList.splice(currentFormIndex, 1);
					}
					if (!responseItem.humanInputFilledFormDataList) responseItem.humanInputFilledFormDataList = [humanInputFilledFormData];
					else responseItem.humanInputFilledFormDataList.push(humanInputFilledFormData);
				});
			},
			onHumanInputFormTimeout: ({ data: humanInputFormTimeoutData }) => {
				updateChatTreeNode(messageId, (responseItem) => {
					if (responseItem.humanInputFormDataList?.length) {
						const currentFormIndex = responseItem.humanInputFormDataList.findIndex((item) => item.node_id === humanInputFormTimeoutData.node_id);
						responseItem.humanInputFormDataList[currentFormIndex].expiration_time = humanInputFormTimeoutData.expiration_time;
					}
				});
			},
			onWorkflowPaused: ({ data: workflowPausedData }) => {
				sseGet(`/workflow/${workflowPausedData.workflow_run_id}/events`, {}, otherOptions);
				updateChatTreeNode(messageId, (responseItem) => {
					responseItem.workflowProcess.status = WorkflowRunningStatus.Paused;
				});
			}
		};
		if (workflowEventsAbortControllerRef.current) workflowEventsAbortControllerRef.current.abort();
		sseGet(url, {}, otherOptions);
	}, [
		updateChatTreeNode,
		handleResponding,
		workflowStore,
		fetchInspectVars,
		invalidAllLastRun,
		config?.suggested_questions_after_answer
	]);
	const handleSwitchSibling = useCallback((siblingMessageId, callbacks) => {
		setTargetMessageId(siblingMessageId);
		const findMessageInTree = (nodes, targetId) => {
			for (const node of nodes) {
				if (node.id === targetId) return node;
				if (node.children) {
					const found = findMessageInTree(node.children, targetId);
					if (found) return found;
				}
			}
		};
		const targetMessage = findMessageInTree(chatTreeRef.current, siblingMessageId);
		if (targetMessage?.workflow_run_id && targetMessage.humanInputFormDataList && targetMessage.humanInputFormDataList.length > 0) handleResume(targetMessage.id, targetMessage.workflow_run_id, callbacks);
	}, [handleResume]);
	return {
		conversationId: conversationId.current,
		chatList,
		setTargetMessageId,
		handleSwitchSibling,
		handleSend,
		handleStop,
		handleRestart,
		handleResume,
		handleSubmitHumanInputForm,
		getHumanInputNodeData,
		isResponding,
		suggestedQuestions
	};
};
//#endregion
//#region app/components/workflow/panel/debug-and-preview/user-input.tsx
var UserInput = () => {
	const workflowStore = useWorkflowStore();
	const inputs = useStore$1((s) => s.inputs);
	const showDebugAndPreviewPanel = useStore$1((s) => s.showDebugAndPreviewPanel);
	const variables = useNodes().find((node) => node.data.type === BlockEnum.Start)?.data.variables || [];
	const visibleVariables = showDebugAndPreviewPanel ? variables : variables.filter((v) => v.hide !== true);
	const handleValueChange = (variable, v) => {
		const { inputs, setInputs } = workflowStore.getState();
		setInputs({
			...inputs,
			[variable]: v
		});
	};
	if (!visibleVariables.length) return null;
	return /* @__PURE__ */ jsx("div", {
		className: cn("relative z-[1] rounded-xl border-[0.5px] border-components-panel-border-subtle bg-components-panel-on-panel-item-bg shadow-xs"),
		children: /* @__PURE__ */ jsx("div", {
			className: "px-4 pb-4 pt-3",
			children: visibleVariables.map((variable, index) => /* @__PURE__ */ jsx("div", {
				className: "mb-4 last-of-type:mb-0",
				children: /* @__PURE__ */ jsx(form_item_default, {
					autoFocus: index === 0,
					payload: variable,
					value: inputs[variable.variable],
					onChange: (v) => handleValueChange(variable.variable, v)
				})
			}, variable.variable))
		})
	});
};
var user_input_default = memo(UserInput);
//#endregion
//#region app/components/workflow/panel/debug-and-preview/chat-wrapper.tsx
var ChatWrapper = ({ ref, showConversationVariableModal, onConversationModalHide, showInputsFieldsPanel, onHide }) => {
	const startVariables = useNodes().find((node) => node.data.type === BlockEnum.Start)?.data.variables;
	const appDetail = useStore((s) => s.appDetail);
	const workflowStore = useWorkflowStore();
	const inputs = useStore$1((s) => s.inputs);
	const setInputs = useStore$1((s) => s.setInputs);
	const initialInputs = useMemo(() => {
		const initInputs = {};
		if (startVariables) startVariables.forEach((variable) => {
			if (variable.default) initInputs[variable.variable] = variable.default;
		});
		return initInputs;
	}, [startVariables]);
	const features = useFeatures((s) => s.features);
	const config = useMemo(() => {
		return {
			opening_statement: features.opening?.enabled ? features.opening?.opening_statement || "" : "",
			suggested_questions: features.opening?.enabled ? features.opening?.suggested_questions || [] : [],
			suggested_questions_after_answer: features.suggested,
			text_to_speech: features.text2speech,
			speech_to_text: features.speech2text,
			retriever_resource: features.citation,
			sensitive_word_avoidance: features.moderation,
			file_upload: features.file
		};
	}, [
		features.opening,
		features.suggested,
		features.text2speech,
		features.speech2text,
		features.citation,
		features.moderation,
		features.file
	]);
	const setShowFeaturesPanel = useStore$1((s) => s.setShowFeaturesPanel);
	const { conversationId, chatList, handleStop, isResponding, suggestedQuestions, handleSend, handleRestart, handleSwitchSibling, handleSubmitHumanInputForm, getHumanInputNodeData } = useChat(config, {
		inputs,
		inputsForm: startVariables || []
	}, [], (taskId) => stopChatMessageResponding(appDetail.id, taskId));
	const handleRestartChat = useCallback(() => {
		handleRestart();
		setInputs(initialInputs);
	}, [
		handleRestart,
		setInputs,
		initialInputs
	]);
	const doSend = useCallback((message, files, isRegenerate = false, parentAnswer = null) => {
		handleSend({
			query: message,
			files,
			inputs: workflowStore.getState().inputs,
			conversation_id: conversationId,
			parent_message_id: (isRegenerate ? parentAnswer?.id : getLastAnswer(chatList)?.id) || void 0
		}, { onGetSuggestedQuestions: (messageId, getAbortController) => fetchSuggestedQuestions(appDetail.id, messageId, getAbortController) });
	}, [
		handleSend,
		workflowStore,
		conversationId,
		chatList,
		appDetail
	]);
	const doRegenerate = useCallback((chatItem, editedQuestion) => {
		const question = editedQuestion ? chatItem : chatList.find((item) => item.id === chatItem.parentMessageId);
		const parentAnswer = chatList.find((item) => item.id === question.parentMessageId);
		doSend(editedQuestion ? editedQuestion.message : question.content, editedQuestion ? editedQuestion.files : question.message_files, true, isValidGeneratedAnswer(parentAnswer) ? parentAnswer : null);
	}, [chatList, doSend]);
	const doSwitchSibling = useCallback((siblingMessageId) => {
		handleSwitchSibling(siblingMessageId, { onGetSuggestedQuestions: (messageId, getAbortController) => fetchSuggestedQuestions(appDetail.id, messageId, getAbortController) });
	}, [handleSwitchSibling, appDetail]);
	const doHumanInputFormSubmit = useCallback(async (formToken, formData) => {
		await handleSubmitHumanInputForm(formToken, formData);
	}, [handleSubmitHumanInputForm]);
	const inputDisabled = useMemo(() => {
		const latestMessage = chatList[chatList.length - 1];
		return latestMessage?.isAnswer && latestMessage.workflowProcess?.status === WorkflowRunningStatus.Paused;
	}, [chatList]);
	const { eventEmitter } = useEventEmitterContextContext();
	eventEmitter?.useSubscription((v) => {
		if (v.type === "WORKFLOW_STOP") handleStop();
	});
	useImperativeHandle(ref, () => {
		return { handleRestart: handleRestartChat };
	}, [handleRestartChat]);
	useEffect(() => {
		if (Object.keys(initialInputs).length > 0) setInputs({
			...initialInputs,
			...inputs
		});
	}, [initialInputs]);
	useEffect(() => {
		if (isResponding) onHide();
	}, [isResponding, onHide]);
	return /* @__PURE__ */ jsxs(Fragment$1, { children: [/* @__PURE__ */ jsx(chat_default, {
		config: {
			...config,
			supportCitationHitInfo: true
		},
		chatList,
		isResponding,
		chatContainerClassName: "px-3",
		chatContainerInnerClassName: "pt-6 w-full max-w-full mx-auto",
		chatFooterClassName: "px-4 rounded-bl-2xl",
		chatFooterInnerClassName: "pb-0",
		showFileUpload: true,
		showFeatureBar: true,
		onFeatureBarClick: setShowFeaturesPanel,
		onSend: doSend,
		inputs,
		inputsForm: startVariables || [],
		onRegenerate: doRegenerate,
		onStopResponding: handleStop,
		onHumanInputFormSubmit: doHumanInputFormSubmit,
		getHumanInputNodeData,
		chatNode: /* @__PURE__ */ jsxs(Fragment$1, { children: [showInputsFieldsPanel && /* @__PURE__ */ jsx(user_input_default, {}), !chatList.length && /* @__PURE__ */ jsx(Empty, {})] }),
		noSpacing: true,
		suggestedQuestions,
		showPromptLog: true,
		chatAnswerContainerInner: "!pr-2",
		switchSibling: doSwitchSibling,
		inputDisabled,
		hideAvatar: true
	}), showConversationVariableModal && /* @__PURE__ */ jsx(ConversationVariableModal, {
		conversationID: conversationId,
		onHide: onConversationModalHide
	})] });
};
ChatWrapper.displayName = "ChatWrapper";
var chat_wrapper_default = memo(ChatWrapper);
//#endregion
//#region app/components/workflow/panel/debug-and-preview/index.tsx
var DebugAndPreview = () => {
	const { t } = useTranslation();
	const chatRef = useRef({ handleRestart: noop });
	const { handleCancelDebugAndPreviewPanel } = useWorkflowInteractions();
	const { handleNodeCancelRunningStatus } = useNodesInteractionsWithoutSync();
	const { handleEdgeCancelRunningStatus } = useEdgesInteractionsWithoutSync();
	const [expanded, setExpanded] = useState(true);
	const nodes = useNodes();
	const selectedNode = nodes.find((node) => node.data.selected);
	const visibleVariables = nodes.find((node) => node.data.type === BlockEnum.Start)?.data.variables || [];
	const [showConversationVariableModal, setShowConversationVariableModal] = useState(false);
	const handleRestartChat = () => {
		handleNodeCancelRunningStatus();
		handleEdgeCancelRunningStatus();
		chatRef.current.handleRestart();
	};
	const workflowCanvasWidth = useStore$1((s) => s.workflowCanvasWidth);
	const nodePanelWidth = useStore$1((s) => s.nodePanelWidth);
	const panelWidth = useStore$1((s) => s.previewPanelWidth);
	const setPanelWidth = useStore$1((s) => s.setPreviewPanelWidth);
	const handleResize = useCallback((width, source = "user") => {
		if (source === "user") localStorage.setItem("debug-and-preview-panel-width", `${width}`);
		setPanelWidth(width);
	}, [setPanelWidth]);
	const { triggerRef, containerRef } = useResizePanel({
		direction: "horizontal",
		triggerDirection: "left",
		minWidth: 400,
		maxWidth: useMemo(() => {
			if (!workflowCanvasWidth) return 720;
			if (!selectedNode) return workflowCanvasWidth - 400;
			return workflowCanvasWidth - 400 - 400;
		}, [
			workflowCanvasWidth,
			selectedNode,
			nodePanelWidth
		]),
		onResize: debounce((width) => {
			handleResize(width, "user");
		})
	});
	return /* @__PURE__ */ jsxs("div", {
		className: "relative h-full",
		children: [/* @__PURE__ */ jsx("div", {
			ref: triggerRef,
			className: "absolute -left-1 top-0 flex h-full w-1 cursor-col-resize resize-x items-center justify-center",
			children: /* @__PURE__ */ jsx("div", { className: "h-10 w-0.5 rounded-sm bg-state-base-handle hover:h-full hover:bg-state-accent-solid active:h-full active:bg-state-accent-solid" })
		}), /* @__PURE__ */ jsxs("div", {
			ref: containerRef,
			className: cn("relative flex h-full flex-col rounded-l-2xl border border-r-0 border-components-panel-border bg-chatbot-bg shadow-xl"),
			style: { width: `${panelWidth}px` },
			children: [/* @__PURE__ */ jsxs("div", {
				className: "system-xl-semibold flex shrink-0 items-center justify-between px-4 pb-2 pt-3 text-text-primary",
				children: [/* @__PURE__ */ jsx("div", {
					className: "h-8",
					children: t("common.debugAndPreview", { ns: "workflow" }).toLocaleUpperCase()
				}), /* @__PURE__ */ jsxs("div", {
					className: "flex items-center gap-1",
					children: [
						/* @__PURE__ */ jsx(tooltip_default, {
							popupContent: t("operation.refresh", { ns: "common" }),
							children: /* @__PURE__ */ jsx(ActionButton, {
								onClick: () => handleRestartChat(),
								children: /* @__PURE__ */ jsx(Icon$2, { className: "h-4 w-4" })
							})
						}),
						visibleVariables.length > 0 && /* @__PURE__ */ jsxs("div", {
							className: "relative",
							children: [/* @__PURE__ */ jsx(tooltip_default, {
								popupContent: t("panel.userInputField", { ns: "workflow" }),
								children: /* @__PURE__ */ jsx(ActionButton, {
									state: expanded ? ActionButtonState.Active : void 0,
									onClick: () => setExpanded(!expanded),
									children: /* @__PURE__ */ jsx(AD, { className: "h-4 w-4" })
								})
							}), expanded && /* @__PURE__ */ jsx("div", { className: "absolute bottom-[-17px] right-[5px] z-10 h-3 w-3 rotate-45 border-l-[0.5px] border-t-[0.5px] border-components-panel-border-subtle bg-components-panel-on-panel-item-bg" })]
						}),
						/* @__PURE__ */ jsx("div", { className: "mx-3 h-3.5 w-[1px] bg-divider-regular" }),
						/* @__PURE__ */ jsx("div", {
							className: "flex h-6 w-6 cursor-pointer items-center justify-center",
							onClick: handleCancelDebugAndPreviewPanel,
							children: /* @__PURE__ */ jsx(P7, { className: "h-4 w-4 text-text-tertiary" })
						})
					]
				})]
			}), /* @__PURE__ */ jsx("div", {
				className: "grow overflow-y-auto rounded-b-2xl",
				children: /* @__PURE__ */ jsx(chat_wrapper_default, {
					ref: chatRef,
					showConversationVariableModal,
					onConversationModalHide: () => setShowConversationVariableModal(false),
					showInputsFieldsPanel: expanded,
					onHide: () => setExpanded(false)
				})
			})]
		})]
	});
};
var debug_and_preview_default = memo(DebugAndPreview);
//#endregion
export { debug_and_preview_default as default };
